# Port targets — `owo-colors-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `owo-colors` that codex-rs uses, and therefore the minimum subset that `owo-colors-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-93] (rank `174`, tier `direct`, status `To Do`)

**Cargo package name**: `owo-colors`
**Rust module name (in `use` statements)**: `owo_colors`

## Summary

- **3** distinct symbols imported by codex-rs
- **11** total `use` statements
- **6** codex source files affected
- **5** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `owo_colors::OwoColorize`

- **6 `use` site(s)** across **5 codex member crate(s)**: `codex-app-server-transport`, `codex-cli`, `codex-cloud-tasks`, `codex-exec`, `codex-state`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server-transport/src/transport/websocket.rs:31` (`codex-app-server-transport`) — `use owo_colors::OwoColorize`
- `cli/src/main.rs:37` (`codex-cli`) — `use owo_colors::OwoColorize`
- `cloud-tasks/src/lib.rs:16` (`codex-cloud-tasks`) — `use owo_colors::OwoColorize`
- `exec/src/event_processor_with_human_output.rs:19` (`codex-exec`) — `use owo_colors::OwoColorize`
- `state/src/bin/logs_client.rs:12` (`codex-state`) — `use owo_colors::OwoColorize`
- `state/src/bin/logs_client.rs:331` (`codex-state`) — `use owo_colors::OwoColorize`

### `owo_colors::Style`

- **3 `use` site(s)** across **2 codex member crate(s)**: `codex-app-server-transport`, `codex-exec`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server-transport/src/transport/websocket.rs:33` (`codex-app-server-transport`) — `use owo_colors::Style`
- `exec/src/event_processor_with_human_output.rs:20` (`codex-exec`) — `use owo_colors::Style`
- `exec/src/event_processor_with_human_output_tests.rs:13` (`codex-exec`) — `use owo_colors::Style`

### `owo_colors::Stream`

- **2 `use` site(s)** across **2 codex member crate(s)**: `codex-app-server-transport`, `codex-cloud-tasks`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server-transport/src/transport/websocket.rs:32` (`codex-app-server-transport`) — `use owo_colors::Stream`
- `cloud-tasks/src/lib.rs:17` (`codex-cloud-tasks`) — `use owo_colors::Stream`
