# Rust callers — `owo-colors-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `owo-colors` | `owo_colors` | 17 | 6 | 2 | 11 |

## `owo-colors`

### Inbound symbols (4 distinct, 17 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `owo-colors`.

#### `owo_colors::OwoColorize` — 10 site(s) (6 codex, 4 other, 6 caller crates)

- ✅ `codex-kotlin/codex-app-server-transport` `src/transport/websocket.rs:31` — `use owo_colors::OwoColorize`
- ✅ `codex-kotlin/codex-cli` `src/main.rs:37` — `use owo_colors::OwoColorize`
- ✅ `codex-kotlin/codex-cloud-tasks` `src/lib.rs:16` — `use owo_colors::OwoColorize`
- ✅ `codex-kotlin/codex-exec` `src/event_processor_with_human_output.rs:19` — `use owo_colors::OwoColorize`
- ✅ `codex-kotlin/codex-state` `src/bin/logs_client.rs:12` — `use owo_colors::OwoColorize`
- ✅ `codex-kotlin/codex-state` `src/bin/logs_client.rs:331` — `use owo_colors::OwoColorize`
- `color-eyre-kotlin/color-eyre` `src/config.rs:9` — `use owo_colors::OwoColorize`
- `color-eyre-kotlin/color-eyre` `src/fmt.rs:3` — `use owo_colors::OwoColorize`
- `color-eyre-kotlin/color-eyre` `src/section/help.rs:8` — `use owo_colors::OwoColorize`
- `color-eyre-kotlin/color-eyre` `tests/theme.rs:96` — `use owo_colors::OwoColorize`

#### `owo_colors::Style` — 4 site(s) (3 codex, 1 other, 3 caller crates)

- ✅ `codex-kotlin/codex-app-server-transport` `src/transport/websocket.rs:33` — `use owo_colors::Style`
- ✅ `codex-kotlin/codex-exec` `src/event_processor_with_human_output.rs:20` — `use owo_colors::Style`
- ✅ `codex-kotlin/codex-exec` `src/event_processor_with_human_output_tests.rs:13` — `use owo_colors::Style`
- `color-eyre-kotlin/color-eyre` `src/config.rs:9` — `use owo_colors::Style`

#### `owo_colors::Stream` — 2 site(s) (2 codex, 0 other, 2 caller crates)

- ✅ `codex-kotlin/codex-app-server-transport` `src/transport/websocket.rs:32` — `use owo_colors::Stream`
- ✅ `codex-kotlin/codex-cloud-tasks` `src/lib.rs:17` — `use owo_colors::Stream`

#### `owo_colors::style` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `color-eyre-kotlin/color-eyre` `src/config.rs:9` — `use owo_colors::style`
